{
  "name": "@activepieces/piece-passgrad-form",
  "version": "0.1.8",
"main": "./src/index.js",
"types": "./src/index.d.ts",
  "description": "Passgrad Form piece — submit form data as flow trigger or collect input mid-flow.",
  "dependencies": {
    "@activepieces/pieces-framework": "*",
    "@activepieces/pieces-common": "*",
    "tslib": "2.6.2"
  },
  "scripts": {
    "build": "tsc -p tsconfig.lib.json && cp package.json dist/"
  }
}
