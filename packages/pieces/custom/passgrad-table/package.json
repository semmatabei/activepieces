{
  "name": "@activepieces/piece-passgrad-table",
  "version": "0.4.0",
  "description": "Passgrad Table piece — trigger flows on record changes, or manage table records from flows.",
  "dependencies": {
    "@activepieces/pieces-framework": "*",
    "@activepieces/pieces-common": "*",
    "tslib": "2.6.2"
  },
  "scripts": {
    "build": "tsc -p tsconfig.lib.json && cp package.json dist/"
  }
}
